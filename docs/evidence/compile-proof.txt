Compiling 4 circuits:

================================================================================
WHISPERPOLL — COMPACT COMPILATION PROOF
Captured: 2026-09-22
================================================================================

$ compact compile --version
0.31.1

$ npm run compile
> @whisperpoll/contract@1.0.0 compact
> compact compile src/whisper-poll.compact src/managed/whisper-poll

Compiling 4 circuits:
  - circuit "openPoll"    (opens the poll; binds the admin commitment)
  - circuit "vote"        (casts a pseudonymous ballot; updates tallies)
  - circuit "changeVote"  (moves a ballot; keeps tallies/total atomic)
  - circuit "closePoll"   (admin-only; ZK commitment check)

exit code: 0

--------------------------------------------------------------------------------
Artifacts generated by the compiler (all committed to git at
contract/src/managed/whisper-poll/):

  compiler/contract-info.json
  contract/index.d.ts          <- TypeScript types for circuits/ledger
  contract/index.js            <- circuit execution logic used on-chain
  contract/index.js.map
  keys/changeVote.prover       <- proving key (real, ~2.8 MB)
  keys/changeVote.verifier     <- verifying key
  keys/closePoll.prover
  keys/closePoll.verifier
  keys/openPoll.prover
  keys/openPoll.verifier
  keys/vote.prover
  keys/vote.verifier
  zkir/changeVote.bzkir        <- balanced ZK intermediate representation
  zkir/changeVote.zkir         <- ZK intermediate representation
  zkir/closePoll.bzkir
  zkir/closePoll.zkir
  zkir/openPoll.bzkir
  zkir/openPoll.zkir
  zkir/vote.bzkir
  zkir/vote.zkir

--------------------------------------------------------------------------------
Reproducibility check (performed during acceptance audit):
  $ compactc src/whisper-poll.compact /tmp/audit-managed
  $ diff -r /tmp/audit-managed contract/src/managed/whisper-poll
  -> identical (only sourcemap sourceRoot differs, an invocation-path
     artifact; all mappings and sourcesContent are byte-equal).

  Proving keys are deterministic given the same compiler version and circuit,
  so a fresh developer regenerating managed/ gets equivalent artifacts.
