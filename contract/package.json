{
  "name": "@whisperpoll/contract",
  "version": "1.0.0",
  "description": "WhisperPoll — anonymous, verifiable polls on the Midnight Network (Compact contract)",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./dist/src/index.js",
  "module": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js",
      "default": "./dist/src/index.js"
    }
  },
  "files": [
    "dist",
    "src/whisper-poll.compact",
    "managed"
  ],
  "scripts": {
    "compact": "compact compile src/whisper-poll.compact src/managed/whisper-poll",
    "build": "rm -rf dist && tsc --project tsconfig.build.json && cp -Rf ./src/managed ./dist/src/managed && cp ./src/whisper-poll.compact ./dist/src",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@midnight-ntwrk/compact-runtime": "0.16.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  }
}
